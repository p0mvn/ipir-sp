# InspiRING.Pack build configuration.
#
# Valar's spiral-rs fork fixes the old single-CRT scalar multiply bug and
# provides a non-AVX-512 NTT path, so this crate no longer forces a target CPU.
# Keep this file as the place for future crate-local cargo settings.
